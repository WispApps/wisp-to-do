Wisp To Do: theme switch and system document access

Cumulative patch for com.wisp.todo; includes the black-theme/contextual-editing update.
Extract over the project root, then assembleDebug and installDebug. Do not uninstall.

Settings: theme switch selects Light/Dark immediately and persists in wisp_settings.
Labels translated for en, pl, uk, de, fr, es, it, pt, nl, ru.
Date/time dialogs and system bar icon appearance follow the selected theme.
Import now uses Android OpenDocument directly; export uses CreateDocument.
The system grants access to the selected document. No app-made file-permission prompt remains.
Cancelling the picker leaves the existing setup/settings screen and reads no new file.
No broad storage permission or all-files access is requested.

Validation: Kotlin/XML parsing, unique resource names, all theme translations, document contracts and null-URI guards. APK/device verification remains necessary (Android SDK unavailable here).

Verify on device: toggle both themes, restart, change all languages, open date/time picker in both themes, cancel import/export from settings and onboarding, restore a real encrypted export, long-press tasks/lists/projects and edit.
