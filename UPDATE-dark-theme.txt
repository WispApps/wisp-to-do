Wisp To Do — Black theme and contextual editing

Apply to the Wisp To Do project (com.wisp.todo), not the earlier app.
Back up app/src/main, then extract this ZIP into the project root with overwrite.
Build: .\gradlew.bat assembleDebug
Install: .\gradlew.bat installDebug
Do not uninstall the app. The package, database schema and encryption format are unchanged.

Changes:
- Black default theme, dark cards, dialogs, date/time pickers, light system icons.
- Long-press a task, list or project to select it. The top bar shows Edit and Delete.
- Tap X or system Back to clear selection. Delete asks for confirmation.
- Rename lists/projects without changing IDs or deleting child tasks.
- Notification: transparent monochrome small icon, original large icon and tap-to-open.
- Launcher icons unchanged.

Checks: Kotlin syntax parser, XML parser, SQL rename/foreign-key preservation and ZIP integrity.
No Android SDK/device available here: APK compilation and runtime checks remain necessary.

Device checks:
1. Long-press items on Home, in projects/lists, search, smart lists and completed tasks.
2. Rename list/group, restart and check child tasks remain.
3. Edit task reminder and verify a newly delivered notification (dismiss older ones).
4. Cancel editor/delete and press Back: selection should clear before navigating away.
5. Check setup, settings and date/time dialogs for contrast.
